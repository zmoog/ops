apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ifq-homelab-kind
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/zmoog/ops.git
    targetRevision: main
    path: apps/ifq/chart
    helm:
      valueFiles:
        - values-homelab-kind.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ifq
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
