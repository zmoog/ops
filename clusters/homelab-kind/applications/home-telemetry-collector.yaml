apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-telemetry-collector-homelab-kind
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/zmoog/ops.git
    targetRevision: main
    path: apps/home-telemetry-collector/chart
    helm:
      valueFiles:
        - values-homelab-kind.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: home-telemetry
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
